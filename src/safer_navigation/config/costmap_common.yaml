# costmap_common.yaml — shared costmap config for move_base

obstacle_range:    2.5
raytrace_range:    3.0
footprint: [[0.30, 0.25], [0.30, -0.25], [-0.30, -0.25], [-0.30, 0.25]]
inflation_radius:  0.55
cost_scaling_factor: 10.0

observation_sources: laser_scan_sensor
laser_scan_sensor:
  sensor_frame:       laser
  data_type:          LaserScan
  topic:              /scan
  marking:            true
  clearing:           true

resolution:    0.05
width:         10.0
height:        10.0
update_frequency:   5.0
publish_frequency:  2.0

static_map:    true
rolling_window: false
